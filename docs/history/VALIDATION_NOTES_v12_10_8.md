# Validation Notes — v12.10.8

Validated in Linux container:

- Python syntax compilation for app modules.
- Core unit tests for redaction engine, package signature, map-field persistence, plugin registry, case comparison, and enterprise audit helper.

Windows EXE build still needs to be run locally with `build_windows_exe.bat` and verified from `dist/`.
