# GeoTrace v12.10.31 Release Hardening v3

This surgical patch fixes the CI failures reported after the v2 release hardening pass.

## Fixed

- Added AI corroboration matrix lines starting with `AI reasoning: Location posture` for deep-context location posture checks.
- Ensured finalized AI summaries include `Suggested first step` whenever an action plan exists.
- Kept filename-only place hints visible as weak candidates while preserving the filename-only confidence cap.
- Updated `APP_BUILD_CHANNEL` and README build channel to `Public Release Candidate`.

## Validation performed

- `python -m compileall -q app tests main.py` completed successfully.
- Targeted pytest checks passed for release files, map false-positive guard, and local vision runner hardening.
- `python tools/audit_release.py` passed after cleaning generated bytecode.
- `python tools/smoke_check.py` passed.

## Notes

- Filename-only location hints remain weak evidence and are not promoted to GPS/coordinate proof.
- Deep-context AI language remains conservative: displayed map/OCR/filename signals are investigative leads until corroborated.
