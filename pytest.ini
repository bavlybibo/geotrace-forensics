[pytest]
addopts = -ra
markers =
    unit: fast deterministic unit tests
    integration: tests that exercise multiple services or file packages
    ui: PyQt/UI smoke tests
    slow: tests that may take longer or require optional external binaries
    privacy: privacy/regression checks for redacted exports
    ai: local AI engine and guardian logic
