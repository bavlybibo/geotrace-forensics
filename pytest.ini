[pytest]
pythonpath = .
testpaths = tests
addopts = -q -p no:ddtrace
filterwarnings =
    ignore:Image.Image.getdata is deprecated:DeprecationWarning

markers =
    ai: AI and deterministic reasoning tests
    unit: fast unit tests
    privacy: privacy and local-first behavior tests
    integration: integration and release-readiness tests
