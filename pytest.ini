[pytest]
pythonpath = .
testpaths = tests
addopts = -q
markers =
    ai: tests covering AI/Guardian scoring and graph helpers
    unit: fast unit tests
    privacy: tests covering privacy-safe export and redaction behavior
    integration: integration-level tests that touch multiple modules
filterwarnings =
    ignore:Image.Image.getdata is deprecated:DeprecationWarning
